<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colortextbutton</item>
        <!-- <item name="colorPrimaryDark">@color/colorPrimaryDark</item> -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colortext</item>
        <item name="android:textColorSecondary">@color/colortext</item>
    </style>

    <style name="Holo" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- no borrar este estilo, se ocupa en el toolbar de escaneo -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- <item name="colorPrimaryDark">@color/colorPrimaryDark</item> -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colortext</item>
        <item name="android:textColorSecondary">@color/colortext</item>
        <item name="android:actionBarSize">12sp</item>
        <item name="actionBarSize">12sp</item>
    </style>

</resources>
